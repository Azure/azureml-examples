# required for notebook testing in workflow actions
# pinned to avoid surprises
# All versions tested locally for Python 3.13 compatibility
ipython-genutils==0.2.0
ipykernel==6.29.5
papermill==2.7.0
pyyaml>=6.0.2
pandas==2.3.3
matplotlib==3.9.0
torch==2.13.0
tensorflow==2.21.0
tensorflow-hub==0.15.0
jupyter-client==8.9.0
# CRITICAL: Pin protobuf FIRST to override azure-ai-ml's protobuf<6 requirement
protobuf>=6.31.1,<8.0.0
azureml-fsspec
# Pre-install strictyaml with Python 3.13 wheel support to prevent build failures
strictyaml>=0.14.1

# Note: Packages NOT included here due to dependency conflicts - notebooks install inline:
# - azure-ai-ml: %pip install azure-ai-ml (simplifies dependency resolution)
# - azureml-mlflow: %pip install azureml-mlflow
# - transformers: %pip install transformers==4.35.2
