type: pipeline_job
jobs:
  hello:
    type: command_job
    command: echo "hello"
    environemnt:
      image: python
    compute: azureml:cpu-cluster
  world:
    type: command_job
    command: echo "world"
    environment:
      image: python
    compute: azureml:cpu-cluster
