type: pipeline_job
jobs:
  hello:
    command: echo "hello"
    environment:
      image: python
    compute: azureml:cpu-cluster
  world:
    command: echo "world"
    environment:
      image: python
    compute: azureml:cpu-cluster
 