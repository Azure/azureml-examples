command: >-
  pip install --upgrade mlflow azureml-mlflow &&
  python -c "import mlflow; mlflow.log_param('hello', 'world')"
environment:
  image: python:3.8
compute: azureml:cpu-cluster

## TODO: remove
environment_variables:
  AZUREML_COMPUTE_USE_COMMON_RUNTIME: "true"