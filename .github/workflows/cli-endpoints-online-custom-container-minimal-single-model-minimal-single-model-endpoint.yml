# This code is autogenerated.
# Code is generated by running custom script: python3 readme.py
# Any manual changes to this file may cause incorrect behavior.
# Any manual changes will be overwritten if the code is regenerated.

name: cli-endpoints-online-custom-container-minimal-single-model-minimal-single-model-endpoint
on:
  workflow_dispatch:
  schedule:
    - cron: "9 6/12 * * *"
  pull_request:
    branches:
      - main
    paths:
      - cli/endpoints/online/custom-container/minimal/single-model/**
      - cli/endpoints/unknown/**
      - infra/bootstrapping/**
      - .github/workflows/cli-endpoints-online-custom-container-minimal-single-model-minimal-single-model-endpoint.yml
      - cli/setup.sh
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: check out repo
      uses: actions/checkout@v2
    - name: azure login
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZUREML_CREDENTIALS}}
    - name: bootstrap resources
      run: |
          bash bootstrap.sh
      working-directory: infra/bootstrapping
      continue-on-error: false
    - name: setup-cli
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash setup.sh
      working-directory: cli
      continue-on-error: true
    - name: validate readme
      run: |
          bash check-readme.sh "${{ github.workspace }}" "${{ github.workspace }}/cli/endpoints/online/custom-container/minimal/single-model"
      continue-on-error: false
    - name: delete endpoint if existing
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          az ml unknown-endpoint delete -n inimalsinglemodelendpoint9177 -y
      working-directory: cli
      continue-on-error: true
    - name: create endpoint
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          cat endpoints\online\custom-container\minimal\single-model\minimal-single-model-endpoint.yml
          az ml unknown-endpoint create -n inimalsinglemodelendpoint9177 -f endpoints\online\custom-container\minimal\single-model\minimal-single-model-endpoint.yml
      working-directory: cli
    - name: cleanup endpoint
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          az ml unknown-endpoint delete -n inimalsinglemodelendpoint9177 -y
      working-directory: cli
