name: cli-scripts-deploy-safe-rollout-amlarc-online-endpoints
on:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * *"
  pull_request:
    branches:
      - main
      - sdk-preview
    paths:
      - cli/endpoints/online/**
      - cli/deploy-safe-rollout-amlarc-online-endpoints.sh
      - .github/workflows/cli-scripts-deploy-safe-rollout-amlarc-online-endpoints.yml
      - cli/setup.sh
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: check out repo
      uses: actions/checkout@v2
    - name: azure login
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZ_CREDS}}
    - name: setup
      run: bash setup.sh
      working-directory: cli
      continue-on-error: true
    - name: scripts installs
      run: sudo apt-get upgrade -y && sudo apt-get install uuid-runtime jq -y
    - name: test script script
      run: set -e; bash -x deploy-safe-rollout-amlarc-online-endpoints.sh
      working-directory: cli
