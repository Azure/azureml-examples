name: action-test
on:
  pull_request:
    branches:
      - main
      - cli-preview
      - releases/current
    paths:
      - .github/workflows/action-test.yml
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: check out repo
      uses: actions/checkout@v2
    - name: test
      run: |
        echo "here"
        echo "there"
        echo "done!"
