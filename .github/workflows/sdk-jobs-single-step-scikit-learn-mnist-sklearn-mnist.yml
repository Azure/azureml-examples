# This code is autogenerated.
# Code is generated by running custom script: python3 readme.py
# Any manual changes to this file may cause incorrect behavior.
# Any manual changes will be overwritten if the code is regenerated.

name: sdk-jobs-single-step-scikit-learn-mnist-sklearn-mnist
# This file is created by sdk/python/readme.py.
# Please do not edit directly.
on:
  workflow_dispatch:
  schedule:
    - cron: "34 9/12 * * *"
  pull_request:
    branches:
      - main
    paths:
      - sdk/python/jobs/single-step/scikit-learn/mnist/**
      - .github/workflows/sdk-jobs-single-step-scikit-learn-mnist-sklearn-mnist.yml
      - sdk/python/dev-requirements.txt
      - infra/bootstrapping/**
      - sdk/python/setup.sh
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: check out repo
      uses: actions/checkout@v2
    - name: setup python
      uses: actions/setup-python@v2
      with:
        python-version: "3.8"
    - name: pip install notebook reqs
      run: pip install -r sdk/python/dev-requirements.txt
    - name: azure login
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZUREML_CREDENTIALS}}
    - name: bootstrap resources
      run: |
          echo '${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}';
          bash bootstrap.sh
      working-directory: infra/bootstrapping
      continue-on-error: false
    - name: setup SDK
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash setup.sh
      working-directory: sdk/python
      continue-on-error: true
    - name: setup-cli
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash setup.sh
      working-directory: cli
      continue-on-error: true
    - name: run jobs/single-step/scikit-learn/mnist/sklearn-mnist.ipynb
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh" generate_workspace_config "../../.azureml/config.json";
          bash "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh" replace_template_values "sklearn-mnist.ipynb";
          [ -f "../../.azureml/config" ] && cat "../../.azureml/config";
          papermill -k python sklearn-mnist.ipynb sklearn-mnist.output.ipynb
      working-directory: sdk/python/jobs/single-step/scikit-learn/mnist
    - name: Determine Failure Reason
      run: |
          cat sklearn-mnist.output.ipynb
          failure_reason="N/A"
          if [ "${{ job.status }}" == "failure" ]; then
            if grep -q "ResourceNotReady" sklearn-mnist.output.ipynb; then
              failure_reason = "ResourceNotReady"
            elif grep -q "quota" sklearn-mnist.output.ipynb; then
              failure_reason="QuotaIssue"
            elif grep -q "ParentResourceNotFound" sklearn-mnist.output.ipynb; then
              failure_reason="ParentResourceNotFound"
            elif grep -q "already exists" sklearn-mnist.output.ipynb; then
              failure_reason="ResourceAlreadyExists"
            elif grep -q "StorageAccountTypeConversionNotAllowed" sklearn-mnist.output.ipynb; then
              failure_reason="InvalidStorageAccount"
            else
              failure_reason="UncategorizedFailure"
            fi
          fi
          echo "FAILURE_REASON=$failure_reason" >> $GITHUB_ENV
      working-directory: sdk/python/jobs/single-step/scikit-learn/mnist
      if: ${{ always() }}
      continue-on-error: true
    - name: Log Job Results to Application Insights
      uses: syedhassaanahmed/app-insights-event-action@v0.1-alpha
      with:
          instrumentation-key: "${{secrets.APP_INSIGHTS_INSTRUMENTATION_KEY}}"
          event-name: "${{ job.status }}_${{ env.FAILURE_REASON }}_${{ github.ref_name }}"
      if: ${{ always() }}
    - name: upload notebook's working folder as an artifact
      if: ${{ always() }}
      uses: actions/upload-artifact@v2
      with:
        name: sklearn-mnist
        path: sdk/python/jobs/single-step/scikit-learn/mnist
