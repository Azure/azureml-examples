# This code is autogenerated.
# Code is generated by running custom script: python3 readme.py
# Any manual changes to this file may cause incorrect behavior.
# Any manual changes will be overwritten if the code is regenerated.

name: sdk-responsible-ai-tabular-responsibleaidashboard-housing-classification-model-debugging-responsibleaidashboard-housing-classification-model-debugging
# This file is created by sdk/python/readme.py.
# Please do not edit directly.
on:
  workflow_dispatch:
  schedule:
    - cron: "59 2/12 * * *"
  pull_request:
    branches:
      - main
    paths:
      - sdk/python/responsible-ai/tabular/responsibleaidashboard-housing-classification-model-debugging/**
      - .github/workflows/sdk-responsible-ai-tabular-responsibleaidashboard-housing-classification-model-debugging-responsibleaidashboard-housing-classification-model-debugging.yml
      - sdk/python/dev-requirements.txt
      - infra/bootstrapping/**
      - sdk/python/setup.sh
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: check out repo
      uses: actions/checkout@v2
    - name: setup python
      uses: actions/setup-python@v2
      with:
        python-version: "3.8"
    - name: pip install notebook reqs
      run: pip install -r sdk/python/dev-requirements.txt
    - name: pip install mlflow reqs
      run: pip install -r sdk/python/mlflow-requirements.txt
    - name: azure login
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZUREML_CREDENTIALS}}
    - name: bootstrap resources
      run: |
          echo '${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}';
          bash bootstrap.sh
      working-directory: infra/bootstrapping
      continue-on-error: false
    - name: setup SDK
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash setup.sh
      working-directory: sdk/python
      continue-on-error: true
    - name: setup-cli
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash setup.sh
      working-directory: cli
      continue-on-error: true
    - name: run responsible-ai/tabular/responsibleaidashboard-housing-classification-model-debugging/responsibleaidashboard-housing-classification-model-debugging.ipynb
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh" generate_workspace_config "../../.azureml/config.json";
          bash "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh" replace_template_values "responsibleaidashboard-housing-classification-model-debugging.ipynb";
          [ -f "../../.azureml/config" ] && cat "../../.azureml/config";
          papermill -k python responsibleaidashboard-housing-classification-model-debugging.ipynb responsibleaidashboard-housing-classification-model-debugging.output.ipynb
      working-directory: sdk/python/responsible-ai/tabular/responsibleaidashboard-housing-classification-model-debugging
    - name: Determine Failure Reason
      run: |
          cat responsibleaidashboard-housing-classification-model-debugging.output.ipynb
          failure_reason="N/A"
          if [ "${{ job.status }}" == "failure" ]; then
            if grep -q "ResourceNotReady" responsibleaidashboard-housing-classification-model-debugging.output.ipynb; then
              failure_reason = "ResourceNotReady"
            elif grep -q "quota" responsibleaidashboard-housing-classification-model-debugging.output.ipynb; then
              failure_reason="QuotaIssue"
            elif grep -q "ParentResourceNotFound" responsibleaidashboard-housing-classification-model-debugging.output.ipynb; then
              failure_reason="ParentResourceNotFound"
            elif grep -q "already exists" responsibleaidashboard-housing-classification-model-debugging.output.ipynb; then
              failure_reason="ResourceAlreadyExists"
            elif grep -q "StorageAccountTypeConversionNotAllowed" responsibleaidashboard-housing-classification-model-debugging.output.ipynb; then
              failure_reason="InvalidStorageAccount"
            else
              failure_reason="UncategorizedFailure"
            fi
          fi
          echo "FAILURE_REASON=$failure_reason" >> $GITHUB_ENV
      working-directory: sdk/python/responsible-ai/tabular/responsibleaidashboard-housing-classification-model-debugging
      if: ${{ always() }}
      continue-on-error: true
    - name: Log Job Results to Application Insights
      uses: syedhassaanahmed/app-insights-event-action@main
      with:
          instrumentation-key: "${{secrets.APP_INSIGHTS_INSTRUMENTATION_KEY}}"
          event-name: "${{ job.status }}_${{ env.FAILURE_REASON }}_${{ github.ref_name }}"
      if: ${{ always() }}
    - name: upload notebook's working folder as an artifact
      if: ${{ always() }}
      uses: actions/upload-artifact@v2
      with:
        name: responsibleaidashboard-housing-classification-model-debugging
        path: sdk/python/responsible-ai/tabular/responsibleaidashboard-housing-classification-model-debugging
